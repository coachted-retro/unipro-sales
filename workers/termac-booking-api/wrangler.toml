name = "termac-booking-api"
main = "index.js"
compatibility_date = "2024-01-01"
workers_dev = true

[vars]
D1_API_URL = "https://unipro-ai-proxy.termac-one.workers.dev"

# Secret (set via the Cloudflare dashboard after this deploys, never
# committed here):
#   D1_API_SECRET — must match the API_SECRET configured on unipro-ai-proxy
#   (the working proxy — a worker literally named "termac-d1-api" does not
#   exist on the account; that was the actual root cause of the digital
#   business card returning "not found" / "unavailable" errors)

# retry trigger
# token refreshed
# trigger after workflow fix
