name = "termac-booking-api"
main = "index.js"
compatibility_date = "2024-01-01"
workers_dev = true

# Secrets (set via `wrangler secret put` or the Cloudflare dashboard, never
# committed here):
#   D1_API_SECRET — must match the API_SECRET configured on termac-d1-api
#
# Plain vars (safe to set in the dashboard or here):
#   D1_API_URL — https://termac-d1-api.termac-one.workers.dev
