# 2026-07-15 per Ted: any link that doesn't match a real page on the
# platform (old bookmark, typo, a renamed/removed portal, a stale link
# someone was handed) now lands on the login page instead of Cloudflare
# Pages' generic 404. Existing real files are matched first and served
# normally -- this only catches genuinely broken paths. staff-login.html
# already sends an already-logged-in user onward to their correct
# portal, so this is safe for authenticated visitors too, not just new
# ones.
/*    /staff-login.html   200
